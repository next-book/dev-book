# dev-book for nb-base development

A shortened and mangled version of Walden for easy build, pulling the JS bundle right out of the nb-base.

```
npm install
npm link nb-mapper
npm link nb-base
npm run build
npm run syncjs
```

While running `npm run build:js-dev` on nb-base.
